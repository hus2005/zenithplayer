name: docker-build

on:
    pull_request:
        paths:
            - '.github/workflows/docker.yml'
            - 'apps/web/**'
            - 'apps/web-backend/**'
            - 'docker/**'
            - 'libs/**'
            - 'package.json'
            - 'pnpm-lock.yaml'
    push:
        branches:
            - master
        tags:
            - 'v*'
    workflow_dispatch:
        inputs:
            push:
                description: 'Push the image to Docker Hub'
                required: true
                type: boolean
                default: false

permissions:
    contents: read

jobs:
    build:
        name: Build Docker image
        runs-on: ubuntu-latest
        timeout-minutes: 90

        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Prepare Docker metadata
              id: docker-meta
              shell: bash
              run: |
                  set -euo pipefail

                  version="$(node -p "require('./package.json').version")"
                  short_sha="${GITHUB_SHA::12}"
                  publish="false"
                  platforms="linux/amd64"
                  tags="hus2005/zenithplayer:pr-${{ github.event.pull_request.number || 'manual' }}"

                  if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/master" ]]; then
                    publish="true"
                    platforms="linux/amd64,linux/arm64"
                    tags=$(cat <<TAGS
                  hus2005/zenithplayer:latest
                  hus2005/zenithplayer:${version}-pwa
                  hus2005/zenithplayer:${version}-pwa-${short_sha}
                  hus2005/zenithplayer:sha-${short_sha}
                  TAGS
                  )
                  elif [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == refs/tags/v* ]]; then
                    publish="true"
                    platforms="linux/amd64,linux/arm64"
                    tag_version="${GITHUB_REF_NAME#v}"
                    tags=$(cat <<TAGS
                  hus2005/zenithplayer:${tag_version}
                  hus2005/zenithplayer:v${tag_version}
                  TAGS
                  )
                    if [[ "${tag_version}" != *-* ]]; then
                      tags="${tags}"$'\n'"hus2005/zenithplayer:stable"
                    fi
                  elif [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" && "${{ inputs.push }}" == "true" ]]; then
                    publish="true"
                    platforms="linux/amd64,linux/arm64"
                    tags=$(cat <<TAGS
                  hus2005/zenithplayer:manual-${short_sha}
                  hus2005/zenithplayer:sha-${short_sha}
                  TAGS
                  )
                  fi

                  {
                    echo "publish=${publish}"
                    echo "platforms=${platforms}"
                    echo "tags<<EOF"
                    echo "${tags}"
                    echo "EOF"
                  } >> "${GITHUB_OUTPUT}"

            - name: Login to Docker Hub
              if: steps.docker-meta.outputs.publish == 'true'
              uses: docker/login-action@v4
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4

            - name: Build Docker image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: ./docker/Dockerfile
                  push: ${{ steps.docker-meta.outputs.publish == 'true' }}
                  tags: ${{ steps.docker-meta.outputs.tags }}
                  platforms: ${{ steps.docker-meta.outputs.platforms }}
                  build-args: |
                      BUILD_COMMIT=${{ github.event.pull_request.head.sha || github.sha }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max,ignore-error=true
